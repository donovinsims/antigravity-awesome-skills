Install and use the SuperMemory MCP server for managing memories.

## Installation

```bash
npx -y install-mcp@latest https://mcp.supermemory.ai/mcp --client gemini-cli --oauth=yes
```

## Usage

Use this skill to integrate SuperMemory into your agent's memory system. It allows storing and retrieving memories via the SuperMemory API.
